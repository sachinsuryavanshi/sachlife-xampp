<?php
/**
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 *
 * @return array
 */
function sachgraph_theme($existing, $type, $theme, $path) {
  return array(
    'sidebar' => array(
      'variables'=> array('content'=>[]),
    ),
  );
}